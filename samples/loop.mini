fun sumUpTo(n: Int): Int {
    var result: Int = 0
    var i: Int = 1
    while (i <= n) {
        result = result + i
        i = i + 1
    }
    return result
}

fun main(): Unit {
    var s: Int = sumUpTo(10)
    println(s)
}
